node{
  stage('Clone') {
    checkout scm
    
    echo "Directory is ${env.GIT_CHECKOUT_DIR}"
    echo ${env.GIT_CHECKOUT_DIR}
    echo ${env.GIT_COMMITTER_NAME}
  }
  stage('Build') {
    echo "Hey!"
  }
    
}
